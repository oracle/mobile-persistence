version.number=12.1.3.2.39

install.dir=${oracle.home}/..
oracle.commons=${install.dir}/oracle_common


home=..
design.dir=${home}/DesignTime

runtime.dir=${home}/Runtime
runtime.build.dir=${runtime.dir}/build

samples.dir=${home}/Tutorial
templates.dir=${home}/templates
doc.dir=${home}/Documentation

datasynch.deploy.dir=${home}/DataSynchFeature/lib
wscalls.deploy.dir=${home}/WebServiceCallsFeature/lib

design.source.dir=${design.dir}/src
design.build.dir=${design.dir}/build
design.icons.dir=${design.dir}/icons
design.lib.dir=${design.dir}/lib
design.deploy.dir=${design.dir}/deploy
jdev.extensions.dir=${oracle.home}/jdev/extensions
design.deploy.jar=${design.deploy.dir}/oracle.ateam.mobile.persistence.jar

design.output.dir=classes


stage.dir=${home}/stage
stage.extension.dir=${stage.dir}/oracle.ateam.mobile.persistence
stage.samples.dir=${stage.extension.dir}/samples
stage.templates.dir=${stage.extension.dir}/templates
stage.icons.dir=${stage.extension.dir}/icons
stage.lib.dir=${stage.extension.dir}/lib
stage.documentation.dir=${stage.extension.dir}/doc
stage.runtime.dir=${stage.extension.dir}
stage.runtime.lib.dir=${stage.runtime.dir}/lib
stage.runtime.javadoc.dir=${stage.runtime.dir}/doc/api
stage.runtime.src.dir=${stage.runtime.dir}/src
stage.project.install.dir=${stage.extension.dir}/install

javac.deprecation=off
javac.debug=on
javac.nowarn=off
